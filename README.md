# Pode jogar?

Pode jogar bola ou não??


## Como Rodar?

1. Entre no diretório `/apis` e execute `python run ./tree/train.py`;
1. Execute a api `uvicorn main:app`;
1. Entre no diretório `/frontend` em outro terminale execute `python -m http.server 8080`;
1. Acesse `http://localhost:8080`;
